To connect run:
    mysql -u root -p  (Note: mysql pw set to same as comp login)

show databases; 


To disconnect run: 
  quit 
  
Note, you can also use [control + Z] is shortcut for [zsh: suspended  mysql -u root -p] 
Neither of these options stops mysql from running, just changes back to main termainal.  




SQL documentation: https://dev.mysql.com/doc/refman/8.0/en/mysql-commands.html

More Notes:
ps - shows all running processes and their IDs 
